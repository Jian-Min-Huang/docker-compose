* my local test docker-compose.yml
```
docker-compose up -d redis
docker-compose up -d mysql
docker-compose up -d oracle
```
```
docker-compose up -d registry.ui
```
```
docker-compose up -d jaeger-query
```
```
docker-compose up -d gitlab
```
